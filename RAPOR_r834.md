SÜKÛN r834 — Jewel dosyalarını tek klasörde toplama

Yeni yer: assets/berhetiyye-premium/

7 ayrı Jewel klasöründeki 119 dosya girdisi birleştirildi. 91 kopya yeniden kullanılabildi; hedef klasöre yalnız 28 yeni fiziksel dosya eklendi. PNG/WebP/SVG görsellerinin içerikleri byte düzeyinde korundu. Yeniden sıkıştırma, kalite veya animasyon değişikliği yok.

Nero, CSS, görsel katalogları ve Service Worker yeni yolları kullanıyor. Yedi eski Jewel klasörü tam paketten kaldırıldı. Sayfa içindeki jewel-r798 gibi sınıf adları CSS kimliğidir; dosya klasörü değildir ve değiştirilmesi gerekmiyor.

102 eski görsel referansının yeni dosyası aynı SHA-256 değerine sahip. 61 yerel kaynak yolu mevcut, 66 önbellek URL'si tekil. 209 stil bloğu ve sürüm notu dışındaki 220 script bloğu yol/sürüm değişiklikleri dışında aynı. Çalıştırılabilir 217 JavaScript bloğu ve SW sözdizimi geçti. 17 bağlam/etkileşim + 11 worker/çevrimdışı kontrolü yeniden geçti. Bu tur fiziksel cihaz veya tarayıcı yerleşim testi yapılmadı.

GitHub yüklemesi:
- r833 kuruluysa SUKUN_r834_GITHUB_GUNCELLEME.zip içindekileri site köküne aynı klasör yapısıyla yükle. Bu ZIP yalnız değişen ve yeni gereken dosyaları içerir.
- Temiz kurulum veya daha eski sürüm için SUKUN_r834_TAM_PAKET.zip kullan.
- Yeni Jewel dosyalarını assets/berhetiyye-premium içine koy. index.html, nero.html, sw.js, manifest ve __sukun_ güncelleme dosyaları kökte kalır; CSS dosyaları css altında kalır.
- Eski Jewel klasörleri r834 tarafından kullanılmaz. Yeni sürümü yükleyip kontrol ettikten sonra eski assets/jewel-* klasörlerini depodan kaldırabilirsin.
- Güncelleme ZIP'ini tek başına temiz bir siteye kurma; r833'ün diğer görsellerini ve içeriklerini kullanır.

Berhetiyye erişim/aktif isim şartı ve Mevlevi görünümüne dönüş r833 ile aynıdır. Canlı site bu işlemde değiştirilmedi.
